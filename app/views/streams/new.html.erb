<div>
  <h1 class="font-bold text-4xl"><%= t('streams.form.title') %></h1>
  <%= form_with model: @stream do |form| %>
    <div class="grid grid-cols-[140px_auto] gap-8 my-8">

    <%= form.submit %>
  <% end %>
</div>
